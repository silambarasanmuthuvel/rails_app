<% if error.errors.any? %>
  <div id="error_explanation" class="alert alert-warning" >
    <h2><%= pluralize(error.errors.count, "error") %> prohibited this topic from being saved:</h2>

    <ul>
      <% error.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>